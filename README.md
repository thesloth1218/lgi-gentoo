# LGI-Gentoo
A simple python based tui installer for gentoo to get to a base install with support for custom kernel .configs as well as auto disk management. Uses ansible for running commands inside and outside the CHROOT enviorment and completes a basic install. Simple enough that a cow could use it to install gentoo!
